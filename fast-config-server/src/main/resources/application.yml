spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://github.com/lisonghua/fast-boot
          search-paths:
          - fast-config-server/src/main/resources
          
server:
  port: 6666